<%- include('../includes/header')%>
<%- include('../includes/navbar')%>
<%- include('../includes/sidebar')%>
<%- include('../includes/start_content_header')%>
<div class="col-sm-6">
      <h1 class="m-0">Users</h1>
  </div><!-- /.col -->
  <div class="col-sm-6">
    <ol class="breadcrumb float-sm-right">
        <li class="breadcrumb-item"><a href="/">Home</a></li>
        <li class="breadcrumb-item active">Users Page</li>
    </ol>
  </div><!-- /.col -->
<%- include('../includes/end_content_header')%>

<%- include('../includes/start_content')%>


<div class="col-12">
	<div class="card">
	<%if(users) {%>
		<div class="card-header">
			<a href="/users/add" class="btn btn-success font-weight-bold">Add user</a>
		</div>
		<div class="card-body">
			<table class="table table-bordered">
			    <thead>
			       <tr>
			            <th>Name</th>
			            <th>Email</th>
			            <th>Role</th>			         
			            <th>phone</th>
			            <th>Opreations</th>
			        </tr>
			   </thead>
			   <tbody>
			   	<% users.forEach(user => {%>
			       <tr data-widget="expandable-table" aria-expanded="false">
			            <td><%= user.name%></td>
			            <td><%= user.email%></td>
			            <td><%= user.role%></td>
			            <td><%= user.phone%></td>
			            <td>
			            	<a href="/users/edit/<%=user.id%>" class="btn btn-primary btn-sm d-inline">Edit</a>
			            	<form action="/users/delete/<%=user.id%>" method='POST' class='d-inline'>
								<input type="hidden" name="_csrf" value="<%= _csrf %>">
			            		<button type="submit" class="btn btn-sm btn-danger">
			            			Delete</button>
			            	</form>
			            	<form action="/users/useStatus/<%=user.id%>" method='POST' class="d-inline">
			            		<input type="hidden" name="_csrf" value="<%= _csrf %>">
			            		<%if(user.useStatus == false) {%>
			            			<button class="btn btn-sm btn-success">Active</button>
			            		<% } else {%>
			            			<button class="btn btn-sm btn-success">UnActive</button>
			            		<%}%>
			            	</form>
			            </td>
					</tr>
			     <%})%>
			   </tbody>
			</table>
		</div>
	<%}%>
	</div>
</div>
<%- include('../includes/end_content')%>

<%- include('../includes/footer')%>